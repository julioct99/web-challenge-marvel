import { Link } from 'react-router-dom'

import styled from 'styled-components'

export const LinkContainer = styled(Link)`
  text-decoration: none;
  color: inherit;
`

export const FavoriteCounterContainer = styled.div`
  display: flex;
  align-items: center;
  gap: 8px;
`
